<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="omicron.app.MainActivity$PlaceholderFragment" 
    android:background="@drawable/vacas">

    
    <Button
        android:id="@+id/btnVerTablas"
        style="@style/botones_inicio"
        android:layout_alignBottom="@+id/btnInformes"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/btnInformes"
        android:drawableTop="@drawable/search4"
        android:text="@string/btnVerTablas"
        android:onClick="openTabbedActivity" />
    
    <Button
        android:id="@+id/btnFiltros"
        style="@style/botones_inicio"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:drawableTop="@drawable/filter2"
        android:text="@string/btnFiltros" />

    <Button
        android:id="@+id/btnInformes"
        style="@style/botones_inicio"
        android:layout_alignBaseline="@+id/btnFiltros"
        android:layout_alignBottom="@+id/btnFiltros"
        android:layout_centerHorizontal="true"
        android:drawableTop="@drawable/navigation"
        android:text="@string/btnInformes" />


</RelativeLayout>
